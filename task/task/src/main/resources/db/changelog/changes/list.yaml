databaseChangeLog:
  - changeSet:
      id: 001_add_table_list_into_database
      context: nominal
      author: n
      changes:
        - sql: CREATE TABLE List(
            List_id SERIAL,
            List_name VARCHAR(100) NOT NULL,
            PRIMARY KEY(list_id)
            );
